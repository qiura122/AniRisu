import 'package:flutter/material.dart';

class HorimiyaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Horimiya')),
      body: Center(child: Text('Ini halaman Horimiya')),
    );
  }
}