import 'package:flutter/material.dart';

class SpyFamPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Spy x Family')),
      body: Center(child: Text('Ini halaman Spy x Family')),
    );
  }
}