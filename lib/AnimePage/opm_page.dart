import 'package:flutter/material.dart';

class OpmPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('One Punch Man')),
      body: Center(child: Text('Ini halaman One Punch Man')),
    );
  }
}